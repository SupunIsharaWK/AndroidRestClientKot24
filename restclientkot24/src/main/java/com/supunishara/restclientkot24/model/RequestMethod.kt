package com.supunishara.restclientkot24.model

/**
 * Enum representing the supported HTTP request methods.
 * Used for type-safe API request building.
 */
enum class RequestMethod {
    GET,
    POST,
    PUT,
    DELETE,
    PATCH
}